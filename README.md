# Simple brainf*ck AOT compiler writen in V
Made for fun. Currently only targets linux x86_64, but other targets could be
implemented through interface.
## Build
Just build it with V compiler:
```sh
v .
```
## Usage example:
```sh
./bfaotc c -target x86_64_linux example.bf
./example
```
